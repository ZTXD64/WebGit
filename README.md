
<h1>This is a new web.I hope I can insist</h1>
``` js
    <script type="module">
        import { createApp, ref } from 'https://unpkg.com/vue@3/dist/vue.esm-browser.js';
            // 创建Vue的app通过creatapp
            createApp({
                data(){
                    return {
                        msg : "hello vue3"
                    }
                }

            }).mount('#app');
            // 通过.mount("id选择器")挂载Vue的app
    
    </script>
```
